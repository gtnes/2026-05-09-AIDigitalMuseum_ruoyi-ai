# 智能体应用对话接口文档

> 对接阿里百炼等平台的智能体/工作流应用，通过 `appId` 切换不同应用，SSE 流式返回对话结果。
>
> **调用流程：** 获取应用列表 → 建立 SSE 连接 → 发送消息 → 接收流式回复

## 

### 1. 获取应用列表
```
GET /system/chatapp/appList
```
返回启用的应用列表（无需登录）。

**响应：**
```json
[
  { "id": 1, "appName": "博物馆AI", "appType": "agent", "providerCode": "dashscope" }
]
```

---

### 2. SSE 连接
```
GET /system/chatapp/connect/{sessionId}
```
建立流式推送连接（无需登录）。

**前端示例：**
```javascript
const eventSource = new EventSource(
  `http://127.0.0.1:6039/system/chatapp/connect/${sessionId}`
);
eventSource.onmessage = (e) => {
  const data = JSON.parse(e.data); // {type: "text|error|done", content: ""}
};
```

---

### 3. 发送对话
```
POST /system/chatapp/chat/send
```
发送消息，AI 回复通过 SSE 推送（无需登录）。

**请求体：**
```json
{
  "appId": 1,
  "message": "你好",
  "sessionId": "session-123"
}
```

| 字段 | 来源 | 说明 |
|------|------|------|
| `appId` | `/appList` 返回的 `id` | 应用ID，切换应用即修改此值 |
| `message` | 用户输入 | 发送的消息内容 |
| `sessionId` | 前端生成 | 与 SSE 连接的 `sessionId` 一致 |

**完整调用：**
```javascript
// 1. 建立 SSE 连接
const sessionId = 'session-' + Date.now();
const es = new EventSource(`http://127.0.0.1:6039/system/chatapp/connect/${sessionId}`);

// 2. 接收消息
let reply = '';
es.onmessage = (e) => {
  const data = JSON.parse(e.data);
  if (data.type === 'text') reply += data.content;
  if (data.type === 'done') es.close();
};

// 3. 发送消息（切换 appId 即可切换应用）
function send(appId, msg) {
  fetch('http://127.0.0.1:6039/system/chatapp/chat/send', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ appId, message: msg, sessionId })
  });
}

// 使用
send(1, '你好');      // 应用1
send(2, '门票价格');  // 应用2（同一个连接）
```

---

### 4. 语音合成（对话回复转语音）

```
POST /voice/tts
```
将文字合成为语音，返回 dataUrl 可直接用 `<audio>` / `new Audio()` 播放（无需登录）。

**请求体：**
```json
{
  "voiceId": 1789500000000000001,
  "text": "你好，欢迎来到云山AI博物馆",
  "timestamp": 1757930000000,
  "nonce": "6ba7b810-9dad-11d1-80b4-00c04fd430c8",
  "sign": "9f3b7c2a4e8d..."
}
```

| 字段 | 来源 | 说明 |
|------|------|------|
| `voiceId` | 博物馆智能体配置 | 音色档案ID，来自 `/system/aimuseum/front/{id}` 返回的 `chatapps[].voiceProfileId` |
| `text` | AI回复文本 | 单次 ≤200 字，长文本请按句分段多次请求 |
| `timestamp` | 前端生成 | 当前毫秒时间戳，与服务器时差须在 ±5 分钟内 |
| `nonce` | 前端生成 | 随机串（建议 UUID），同一 nonce 5分钟内仅允许提交一次 |
| `sign` | 前端计算 | HMAC-SHA256 签名，算法见下 |

**响应：**
```json
{ "code": 200, "data": { "dataUrl": "data:audio/mp3;base64,//uQx...", "format": "mp3", "textLength": 15 } }
```

**签名算法（与前端 src/api/app-chat/index.ts 保持一致）：**
```
sign = hex( HMAC-SHA256( 密钥, `${timestamp}\n${nonce}\n${text}` ) )
```
- 密钥：与后端 `application.yml` 中 `tts.sign-secret` 保持一致
- 换行符为真实的 `\n`（LF），密钥更换时前后端需同步修改

**防滥用规则（后端 TtsRequestGuard 校验，任一不通过即拒绝）：**

| 防护层 | 规则 | 触发提示 |
|--------|------|----------|
| 签名校验 | 时间窗 ±5 分钟 + HMAC 一致 | "语音请求已过期" / "签名校验失败" |
| nonce防重放 | 同一 nonce 5分钟内仅一次 | "请勿重复提交语音请求" |
| IP频率限制 | 单 IP 每分钟最多 30 次 | "语音合成请求过于频繁" |
| IP日配额 | 单 IP 每日最多 100 次 | "今日语音合成次数已达上限" |
| 文本长度 | 单次 ≤200 字 | "单次合成文本过长" |

> 注：签名密钥在前端 JS 中可见，仅用于抬高直刷门槛，真正的成本兜底是 IP 频率与日配额。
> 签名开关可在 `application.yml` 中 `tts.sign-enabled: false` 临时关闭（限流仍生效）。

**前端调用示例：**
```javascript
const TTS_SIGN_SECRET = '与后端tts.sign-secret一致';

async function signTts(text) {
  const timestamp = Date.now();
  const nonce = crypto.randomUUID();
  const enc = new TextEncoder();
  const key = await crypto.subtle.importKey(
    'raw', enc.encode(TTS_SIGN_SECRET),
    { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']
  );
  const sig = await crypto.subtle.sign('HMAC', key,
    enc.encode(`${timestamp}\n${nonce}\n${text}`));
  const sign = Array.from(new Uint8Array(sig))
    .map(b => b.toString(16).padStart(2, '0')).join('');
  return { timestamp, nonce, sign };
}

async function tts(voiceId, text) {
  const payload = await signTts(text);
  const res = await fetch('http://127.0.0.1:6039/voice/tts', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ voiceId, text, ...payload })
  }).then(r => r.json());
  // res.data.dataUrl 直接播放
  if (res.code === 200) new Audio(res.data.dataUrl).play();
}
```

---

## 管理接口（需登录）

| 接口 | 方法 | 权限 |
|------|------|------|
| /system/chatapp/list | GET | system:chatapp:list |
| /system/chatapp/{id} | GET | system:chatapp:query |
| /system/chatapp | POST | system:chatapp:add |
| /system/chatapp | PUT | system:chatapp:edit |
| /system/chatapp/{ids} | DELETE | system:chatapp:remove |
| /system/chatapp/export | POST | system:chatapp:export |